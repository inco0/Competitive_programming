#include <iostream>
#include <vector>
#include <iterator>
#include <math.h>

using namespace std;

void solve(){
	int n;
	cin >> n;
}

int main(){
	ios::sync_with_stdio(false);
	cin.tie(0);
	solve();
}