#include <iostream>
#include <vector>
#include <iterator>
#include <math.h>

using namespace std;

#define ll long long

void solve(){
	int n;
	cin >> n;
	for (int i=0; i < pow(2,n); i++){
		
	}
	
}

int main(){
	ios::sync_with_stdio(false);
	cin.tie(0);
	solve();
}